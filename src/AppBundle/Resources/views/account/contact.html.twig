{% extends '::frontend.html.twig' %}

{% block title %}{% endblock %}

{% block article %}
  <div class="content">
    <h1>{{ "contact.article" | trans({}, 'forms') }}</h1>
      <form action="{{ path('contact') }}" method="post" >
        {{ form_widget(form) }}
      </form>
  </div>
{% endblock %}

